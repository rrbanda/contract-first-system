apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: contract-first-springbootdemo-pipeline
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    system: contract-first
spec:
  project: default
  source:
    repoURL: https://github.com/rrbanda/developer-charts
    targetRevision: HEAD
    path: spring-boot-pipeline
    helm:
      values: |
        domain: rrbanda
        system: contract-first
        component: springbootdemo
  destination:
    server: https://kubernetes.default.svc
    namespace: contract-first-build
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=false
    - RespectIgnoreDifferences=true
    - ApplyOutOfSyncOnly=true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 10m